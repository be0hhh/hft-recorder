#pragma once

// hft-recorder — compile-time version string.
// Bumped manually; kept in sync with project(... VERSION ...) in root CMakeLists.txt.

namespace hftrec {

inline constexpr const char* kHftRecorderVersion = "0.1.0";

}  // namespace hftrec
