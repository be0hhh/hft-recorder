import QtQuick
import QtQuick.Controls

Pane {
    Column {
        spacing: 12
        anchors.fill: parent
        anchors.margins: 16
        Label { text: "Compression Lab" }
        Label { text: "Run baseline and custom pipelines against a recorded session." }
    }
}
