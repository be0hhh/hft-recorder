# src/ — top-level tree for hft-recorder.
# Layout per doc/SOURCE_LAYOUT_AND_VARIANTS.md.

add_subdirectory(core)
add_subdirectory(support)
add_subdirectory(variants)
add_subdirectory(app)
add_subdirectory(gui)
