# Superseded: hft-recorder Local Exchange Future Plan

The current code checklist for `hftrecorder_local` is now in the repository root:

`../../CXETCPP_LOCAL_EXCHANGE_CODE_TODO.md`

Use that file as the implementation source of truth. This file is kept only so older references do not break.