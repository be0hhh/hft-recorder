# Backlog

Purpose:

- Hold important next tasks that are not active yet.

Backlog:

- `define recorder qdrant ingest set`
  - owner: `agent tooling`
  - status: `backlog`
  - notes: start with decisions, gotchas, runtime summaries, and selected file-role summaries

- `define external docs grounding`
  - owner: `agent tooling`
  - status: `backlog`
  - notes: useful for third-party APIs, not for local project truth

- `refresh lab/variants map after real implementation work`
  - owner: `lab/variants`
  - status: `backlog`
  - notes: current notes mark these areas as partly transitional

- `add explicit done/archive note if task volume grows`
  - owner: `obsidian`
  - status: `backlog`
  - notes: only needed if task history becomes noisy

- `link recorder linear issue summaries later`
  - owner: `agent tooling`
  - status: `backlog`
  - notes: only after recorder project structure in `Linear` stabilizes

- `decide github mcp usage for recorder reviews`
  - owner: `agent tooling`
  - status: `backlog`
  - notes: only if recorder work starts leaning on GitHub review flow
