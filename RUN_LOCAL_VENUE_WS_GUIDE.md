# Superseded: Local Venue WebSocket Guide

The current code checklist for `hftrecorder_local` is now in the repository root:

`../../CXETCPP_LOCAL_EXCHANGE_CODE_TODO.md`

Use that file for the remaining implementation work. Create a fresh runbook only after the end-to-end local exchange path is verified.